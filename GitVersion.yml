assembly-versioning-scheme: MajorMinorPatchTag
mode: ContinuousDeployment
branches:
  feature:
    mode: ContinuousDeployment
    tag: beta
    increment: Patch
    regex: ^feature?[/-]
  main:
    mode: ContinuousDeployment
    increment: Minor
    regex: ^main$
ignore:
  sha: []
merge-message-formats: {}
